service: sentenceio

provider:
  lambdaHashingVersion: 20201221
  name: aws
  runtime: provided
  region: eu-central-1

functions:
  sentenceio:
    handler: bootstrap
    package:
      artifact: ./target/x86_64-unknown-linux-musl/release/bootstrap
      individually: true
      exclude:
        - '**/**'
      include:
        - ./target/**
    events:
      - httpApi:
          path: /
          method: POST
          request:
            template: application/json