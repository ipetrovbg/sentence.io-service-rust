service: sentenceio

provider:
  lambdaHashingVersion: 20201221
  name: aws
  runtime: provided
  region: eu-central-1

functions:
  sentenceio:
    handler: bootstrap
    package:
      artifact: ./build/bootstrap.zip
      individually: true
      exclude:
        - '**/**'
      include:
        - ./build/**
    events:
      - httpApi:
          path: /
          method: POST
          request:
            template: application/json